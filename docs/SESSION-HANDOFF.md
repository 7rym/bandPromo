# Session Handoff

## Resume point

**Published / publishing:** legacy stage port + tag-fill + visual wipe + Treat liveness. After Site update on HITZ: Quick check → Review → Apply (Untitled heal + visuals stay), then optional Force.

### Still open after publish

1. **HITZ Treat** — prove Untitled tag-fill and that visuals stay registered.
2. **Cover extract** into `treat_audio`.
3. Re-smoke Force on Vanilla / Spandexual after they pick up this build.

### Local workspace

Checkout is **`C:\dev\bandpromo`**. Never wipe `data/` / `media/` / `log/` / `backups/` here.
