# Session Handoff

## Resume point

**Published pending:** drop redundant post-update “tune-up recommended” notification (auto Quick check already runs). Clear `package_update` build-required on Site health start.

### Local workspace

Checkout is **`C:\dev\bandpromo`**. Never wipe `data/` / `media/` / `log/` / `backups/` here.
