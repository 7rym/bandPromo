# Session Handoff

## Resume point

**Manual row chrome polish published (build 540)** — fleet testing Site health Adopt/Delete + clash UX.

### Shipped recently

1. Data janitor (prune / relink / Manual Adopt|Delete).
2. Orphan clash preview + select-first Set.
3. Post-update “already running” modal fix.
4. Proposed treatment Manual rows: quieter separators, Adopt/Set hidden until choice, soft Delete.

### Next

1. Fleet feedback on Status / Site health Manual rows.
2. Optional: teach `orphan_homes` to ignore invisible docs once Adopt/Delete is live.
3. Optional: further Status tools beside Site health.

### Local workspace

Checkout is **`C:\dev\bandpromo`**. Never wipe `data/` / `media/` / `log/` / `backups/` here.

### Fleet testing note

Operator is testing the fleet — treat “check and publish” as the default checkpoint for Status/Site health work in this stretch.
