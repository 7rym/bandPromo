# Session Handoff

## Resume point

**Post-update “already running” modal fixed** — published with data janitor / clash UX; next session can take operator feedback.

### Shipped

1. Multi-campaign orphan clash preview + select-first Set.
2. Site health data janitor (prune / relink / Manual Adopt|Delete).
3. Post-update double auto-start no longer pops “Site health is already running” as an error modal.

### Next

1. Operator feedback on Status / Site health.
2. Optional: teach `orphan_homes` to ignore invisible docs once Adopt/Delete is live.
3. Optional: further Status tools beside Site health.

### Local workspace

Checkout is **`C:\dev\bandpromo`**. Never wipe `data/` / `media/` / `log/` / `backups/` here.
