name: "Template integrity"

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-templates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate required template files and JSON syntax
        shell: bash
        run: |
          set -euo pipefail

          required=(
            "biblioteca/templates/web-config.template.json"
            "biblioteca/templates/default.release.template.json"
            "biblioteca/templates/pages.registry.template.json"
            "biblioteca/templates/bio.template.json"
            "biblioteca/templates/faq.template.json"
            "biblioteca/templates/legacy/gallery-flat-array.json"
          )

          for f in "${required[@]}"; do
            if [ ! -f "$f" ]; then
              echo "Missing required template: $f"
              exit 1
            fi
          done

          python -m json.tool biblioteca/templates/web-config.template.json >/dev/null
          python -m json.tool biblioteca/templates/default.release.template.json >/dev/null
          python -m json.tool biblioteca/templates/legacy/gallery-flat-array.json >/dev/null
          python -m json.tool biblioteca/templates/pages.registry.template.json >/dev/null
          python -m json.tool biblioteca/templates/bio.template.json >/dev/null
          python -m json.tool biblioteca/templates/faq.template.json >/dev/null

          echo "Template integrity checks passed."
