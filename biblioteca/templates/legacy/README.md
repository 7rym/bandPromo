# Legacy template shapes

Reference-only JSON for one-shot import migrations. These files are **not** seeded on fresh installs.

| File | Former path | Purpose |
|------|-------------|---------|
| `gallery-flat-array.json` | `gallery.template.json` | Flat array shape for legacy `data/gallery.json` import via `bandpromo_gallery_migrate_from_legacy()` |
